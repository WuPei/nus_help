
has choosed 
<% if activity.trackable.helper == current_user %>
	you 
<% else %>
<strong><%= link_to activity.trackable.helper.name,activity.trackable.helper %></strong>
<% end %>
as helper! 
<% if activity.trackable.helper == current_user %>
	You could go to <%= link_to "comment", activity.trackable.post.title%> the user if you recived the reward.
<% else %>
	Go to have a look on it <%= link_to activity.trackable.post.title %>
<% end %>