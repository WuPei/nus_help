<% size = 100 if size.nil?; need_div = true if need_div.nil?; %>
<% if need_div %> <% end %>
  <%= link_to (image_tag user.cropped_img_url , :size => "%dx%d" % [size,size], :crop => :fill), user%>
<% if need_div %>  <% end %>

