<% if feed_items.any? %>
  <ul class="nav nav-list" id="homehelpFeeds">
    <%= render partial: 'shared/feed_item', collection: feed_items %>
  </ul>
  <%= will_paginate  feed_items%>
<% else %>
  <em>There's no feed yet.</em>
<% end %>