<% provide(:title, 'Modules') %>
<h1>Modules</h1>

<%= render "user_modules" %>

<% if @nus_modules.any? %>
  <%= will_paginate %>
  <%= render @nus_modules %>
  <%= will_paginate %>
<% else %>
  Sorry, there is no modules available now.
  <%= link_to "Create a module now!", createmod_path, 
                                class: "btn btn-large btn-primary" %>
<% end %>
